<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HaloPixelToolBox.Views.CloudMusicLyricsToolPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <Style x:Key="SectionStyle" BasedOn="{StaticResource BodyStrongTextBlockStyle}" TargetType="TextBlock">
            <Style.Setters><Setter Property="Margin" Value="1,24,0,6"/></Style.Setters>
        </Style>
    </Page.Resources>

    <Grid>
        <ScrollView>
            <StackPanel Orientation="Vertical" Spacing="10" Margin="12,12,12,20">

                <Grid RowSpacing="10" ColumnSpacing="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="音响状态：" VerticalAlignment="Center" HorizontalAlignment="Right" Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                    <Grid Grid.Column="1">
                        <Border CornerRadius="90" Height="25" Width="25"
                                Visibility="{x:Bind ViewModel.DeviceReady, Mode=OneWay, Converter={StaticResource BooleanInverseConverter}}"
                                Background="{ThemeResource SystemFillColorCriticalBrush}"/>
                        <Border CornerRadius="90" Height="25" Width="25"
                                Visibility="{x:Bind ViewModel.DeviceReady, Mode=OneWay}"
                                Background="{ThemeResource SystemFillColorSuccessBrush}"/>
                    </Grid>
                </Grid>

                <TextBlock Text="显示模式" Style="{StaticResource SectionStyle}"/>
                <ComboBox ItemsSource="{x:Bind ViewModel.LayoutOptions}"
                          SelectedIndex="{x:Bind ViewModel.SelectedLayoutIndex, Mode=TwoWay}"/>

                <TextBlock Text="自定义文字" Style="{StaticResource SectionStyle}"/>
                <TextBox Text="{x:Bind ViewModel.TestText, Mode=TwoWay}"
                         PlaceholderText="输入要显示在音箱上的文字（最长50字符）..."
                         MaxLength="50"/>
                <Button x:Name="sendBtn" Click="SendBtn_Click"
                        Style="{StaticResource AccentButtonStyle}"
                        HorizontalAlignment="Stretch" Height="40">
                    <TextBlock Text="发送到音箱"/>
                </Button>
                <TextBlock Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                           Style="{StaticResource BodyTextBlockStyle}"
                           Foreground="{ThemeResource SystemFillColorSuccessBrush}" TextWrapping="Wrap"/>

            </StackPanel>
        </ScrollView>
    </Grid>
</Page>
